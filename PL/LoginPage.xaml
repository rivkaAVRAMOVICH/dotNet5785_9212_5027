<Window x:Class="PL.LoginPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="LoginPage" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Id:" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox x:Name="IdTextBox" Grid.Row="1" Width="200" Margin="0,5,0,10"/>
      
        <TextBlock Text="Password:" Grid.Row="2" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Width="200" Margin="0,5,0,10"/>

        <Button x:Name="LoginButton" Content="התחבר" Grid.Row="4" Width="100" Height="30"
                HorizontalAlignment="Center" Click="LoginButton_Click"/>
    </Grid>
</Window>
